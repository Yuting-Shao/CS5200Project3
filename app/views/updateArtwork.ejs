<!DOCTYPE html>
<html>

<head>
    <title>Update Artwork</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <div class="container">
        <h1>Update Artwork</h1>
        <form action="/artwork/<%= artwork._id %>?_method=PUT" method="POST">
            <!-- ID (Read-only) -->
            <div>
                <label>ID: <input type="text" name="_id" value="<%= artwork._id %>" readonly /></label>
            </div>

            <!-- Title -->
            <div>
                <label>Title: <input type="text" name="title" value="<%= artwork.title %>" /></label>
            </div>

            <!-- Medium -->
            <div>
                <label>Medium: <input type="text" name="medium" value="<%= artwork.medium %>" /></label>
            </div>

            <!-- Dimensions -->
            <div>
                <label>Dimensions: <input type="text" name="dimension" value="<%= artwork.dimension %>" /></label>
            </div>

            <!-- Creation Date -->
            <div>
                <label>Creation Date: <input type="text" name="creationDate"
                        value="<%= artwork.creationDate %>" /></label>
            </div>

            <!-- Price -->
            <div>
                <label>Price: <input type="number" name="price" value="<%= artwork.price %>" /></label>
            </div>

            <!-- Availability Status -->
            <div>
                <label>Availability Status: <input type="text" name="availabilityStatus"
                        value="<%= artwork.availabilityStatus %>" /></label>
            </div>

            <!-- Artist ID (Read-only if present) -->
            <div>
                <label>Artist ID: <input type="text" name="artistId"
                        value="<%= artwork.artist ? artwork.artist._id : '' %>" readonly /></label>
            </div>

            <!-- Submit Button -->
            <div>
                <input type="submit" value="Update" />
            </div>
        </form>
    </div>
    <footer>
        <div> Made by Yuting</div>
    </footer>
</body>

</html>